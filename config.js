const CONFIG = {
  startSlide: 2,        // always skip title slide
  endSlide: 15,         // update if slide count changes
  interval: 6000,       // 6 seconds per slide
  refreshInterval: 60000 // reload every 60 sec to detect updates
};
